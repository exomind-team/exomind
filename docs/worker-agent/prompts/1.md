# 第 1 步

> 仅供参考：该文件仅作为流程参考，不再作为循环输入提示词。实际入口是 `docs/worker-agent/prompts/main.md`。

你现在是 `工作 Agent`。先恢复当前上下文，不要直接改代码。

执行要求：

1. 优先尝试运行：
   - `npx tsx Scripts/dev/worker-agent/index.ts restore`
   - 若环境提供 bun，也可使用：`bun Scripts/dev/worker-agent/index.ts restore`
2. 读取当前 PR、分支、worktree、基线分支、head SHA、等待状态。
3. 若没有当前 PR 锁，明确说明当前无法恢复，需要先进入建 PR / 上锁流程。
4. 输出一个精简状态摘要。

完成条件：

- 已确认当前持锁 PR 是谁，或明确说明当前没有锁。
