# Step 5

> Reference only: 该文件仅作为流程参考，不再作为循环输入提示词。实际入口是 `docs/worker-agent/prompts/main.md`。

你现在处理“继续推进当前任务开发”。

执行要求：

1. 只有在当前 `head SHA` 上没有未处理阻塞项时，才能进入这一步。
2. 只推进当前 issue / 当前 PR 范围内的工作。
3. 改代码前，先明确本轮要完成的最小目标。
4. 不要顺手扩写无关功能。

完成条件：

- 已完成本轮最小代码改动，准备进入验证阶段。
