# 第 6 步

> 仅供参考：该文件仅作为流程参考，不再作为循环输入提示词。实际入口是 `docs/worker-agent/prompts/main.md`。

你现在处理“验证、提交、推送、评论回传”。

执行要求：

1. 先同步 PR 正文，确保描述仍准确。
2. 运行受影响测试、类型检查、必要构建。
3. 测试通过后再提交与推送。
4. 所有对外文本都跟随 `next-action` / `restore` 输出中的 `context.targetLanguage`。
5. 使用 `[Codex Worker]` 评论回传：
   - 变更 / Change
   - 验证 / Verification
   - 结果 / Result
6. 发布前，优先尝试：
   - `npx tsx Scripts/dev/worker-agent/index.ts validate-message --file <path>`

完成条件：

- 代码已推送，评论已发出，PR 正文已同步。
