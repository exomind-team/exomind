# docs/agents/ — 已迁移

本目录下的内容已迁移至项目根目录：

- **Skills**（可调用的流程/能力）→ `/skills/`
  - dev-daily, dev-route, issue-triage, issue-tracking
- **Agents**（自主运行的 agent）→ `/agents/`
  - review-agent

Claude Code 通过 `.claude/skills` 和 `.claude/agents` 符号链接发现它们。

详见 [Issue #736](https://github.com/exomind-team/exomind/issues/736)。
