/**
 * Legacy TaskSyncCoordinator has been retired.
 * Task now uses RT SQLite + mesh/backfill as the main sync path（Task 已切到 RT SQLite 主链路）.
 */
export function TaskSyncCoordinator(): null {
  return null;
}
