# ExoMind v0.3.5 Release Finalization Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Merge `feature/issue-354-351-history-chat-ci` into `dev`, pass local verification gates, publish `release/v0.3.5`, and verify website download APIs resolve install packages.

**Architecture:** Use the existing release automation in `.github/workflows/release.yml` as the single source of truth for artifacts and metadata. Ship from Git tag `release/v0.3.5`, then verify both GitHub Release assets and website API endpoints backed by R2 metadata (`release/latest.json`).

**Tech Stack:** Git, GitHub Actions, Bun/Node, Tauri, Vitest, Astro API routes, Cloudflare R2/Workers integration.

---

### Task 1: Prepare Release Baseline

**Files:**
- Modify: `.git` refs only (no product code changes expected)

**Step 1: Sync refs**

Run: `git fetch origin --prune`
Expected: remote refs are updated with no local conflicts

**Step 2: Check branch delta**

Run: `git log --oneline --left-right origin/dev...feature/issue-354-351-history-chat-ci`
Expected: only intended commits exist on feature side

**Step 3: Update local dev**

Run: `git checkout dev && git pull --ff-only origin dev`
Expected: local `dev` is aligned with `origin/dev`

**Step 4: Merge release content into dev**

Run: `git merge --ff-only feature/issue-354-351-history-chat-ci`
Expected: fast-forward succeeds (or stop and resolve if history diverges)

**Step 5: Push baseline**

Run: `git push origin dev`
Expected: remote `dev` contains release baseline commit(s)

### Task 2: Run Local Verification Gates

**Files:**
- Verify only: `src/`, `server/`, `website/`, `src-tauri/` (build/test gates)

**Step 1: Type check**

Run: `npx tsc --noEmit`
Expected: exit code 0

**Step 2: Targeted test suite**

Run: `npx vitest run src/store/__tests__/agentStore.history.test.ts src/store/__tests__/agentStore.chat.test.ts src/store/__tests__/agentStore.lifecycle.test.ts src/lib/__tests__/keyboardShortcut.android.test.ts`
Expected: all tests pass

**Step 3: Native compile gate**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`
Expected: exit code 0

**Step 4: Website build gate**

Run: `bun run website:install && bun run website:build`
Expected: website build succeeds with no blocking errors

### Task 3: Publish release/v0.3.5

**Files:**
- Modify: git tag refs only

**Step 1: Create tag**

Run: `git tag release/v0.3.5`
Expected: local tag created

**Step 2: Push tag**

Run: `git push origin release/v0.3.5`
Expected: remote tag exists and triggers `Build & Release`

**Step 3: Monitor CI**

Run: `gh run list --workflow "Build & Release" --limit 5`
Expected: a run for `release/v0.3.5` appears

**Step 4: Wait for completion**

Run: `gh run watch <run-id>`
Expected: all required jobs complete successfully

### Task 4: Verify Download Distribution

**Files:**
- Verify: `website/src/pages/api/versions.ts`
- Verify: `website/src/pages/api/download/[channel]/[version]/[platform].ts`
- Verify: Cloud R2 metadata produced by workflow

**Step 1: Check GitHub release object**

Run: `gh release view release/v0.3.5 --json tagName,publishedAt,url,assets`
Expected: release exists with expected assets

**Step 2: Check versions API**

Run: `curl "https://exo-mind.ai/api/versions?channel=release"`
Expected: latest points to `v0.3.5` (or normalized `0.3.5`)

**Step 3: Check download API redirects**

Run: `curl -I "https://exo-mind.ai/api/download/release/v0.3.5/windows-x64-setup"`
Expected: `302`/`307` to a valid asset URL

**Step 4: Check Android package availability**

Run: `curl -I "https://exo-mind.ai/api/download/release/v0.3.5/android-arm64"`
Expected: `302`/`307` to `.apk` asset URL

### Task 5: Update Issue and Delivery Summary

**Files:**
- Modify: GitHub issue comment/update only

**Step 1: Summarize delivered scope vs v0.3.5 issue checklist**

Run: compare issue checklist with commit `315bc93`
Expected: completed/incomplete items are explicit

**Step 2: Post issue update**

Run: `gh issue comment <issue-id> --body-file <summary-file>`
Expected: issue has timestamped execution update

**Step 3: Publish final operator report**

Run: provide report with command evidence + links
Expected: reproducible release evidence for build/test/release/download
