# 2026-04-06 独立集体记账系统第一阶段计划

> 状态：已收口子计划
>
> 目的：把“集体记账 / Labor Ledger”从外心主线中拆出，明确第一阶段先做一个独立系统，再讨论如何并入外心。

---

## 1. 计划摘要

`集体记账 / Labor Ledger` 近期不作为外心内的数据域推进，而先收口为：

- 一个**独立配套系统**
- 目标是**快速承接旧有记账功能**
- 同时尽可能为后续并入外心保留结构条件

这意味着第一阶段不讨论它如何直接嵌进档案 / 集体档案 / 信号网络，而先把独立系统本身做出来。

---

## 2. 第一阶段定位

### 2.1 不是当前外心主线内的数据域

第一阶段中，记账系统的定位是：

- **外部配套系统**

而不是：

- 立即并入外心主线
- 立即作为集体档案中的一个内建对象域

### 2.2 先独立开发，再讨论接入

当前阶段的顺序固定为：

1. 先把独立系统做出来
2. 再讨论如何纳入外心系统

---

## 3. 参考系统与复刻边界

当前参考系统来自：

- `G:\Downloads\集体记账管理 飞书多维表格`

已确认可见骨架包括：

- 数据表：
  - `账单总表`
  - `收支分类定义`
  - `平账总表`
- 工作面 / 面板：
  - `快速记账`
  - `平账`
  - `收支统计`
  - `均线看盘`

第一阶段的复刻深度收口为：

- **核心流程优先**

也就是说：

- 先复刻记账真相与核心工作流
- 不要求首版把所有统计 / 看盘视图一比一搬完

---

## 4. 第一阶段必须进入的工作面

独立系统第一版必须先承接：

- `账单总表`
- `收支分类定义`
- `快速记账`
- `平账`

后续再补：

- `收支统计`
- `均线看盘`

---

## 5. 数据与架构策略

### 5.1 交付形态

第一版交付形态固定为：

- **Rust 内核 + 简 Web UI**

### 5.2 数据策略

为满足“先承接旧功能，后续又能并入外心”，数据策略收口为：

- **兼容外层，规范内核**

具体解释：

- 不要求导入旧表历史数据
- 但要尽可能实现旧表的数据结构与运作逻辑
- 对外工作面尽量贴近参考系统
- 内核提前整理为更适合后续并入外心的 Rust 域模型

### 5.3 权威账本形态

第一阶段的权威数据模型收口为：

- **事件账本 + 投影**

即：

- 原始层使用追加友好的文本格式
- 当前默认方向是 `JSONL`
- 账单表 / 平账表 / 统计视图是投影层，不是权威源

### 5.4 存储与同步策略

第一阶段不采用本地 `SQLite` 作为主存储，而收口为：

- 使用**能承载表格信息的文本文件**
- 通过 **GitHub 集体私有仓库** 做同步

进一步收口为：

- 权威事件文件按**年-月**分区
- Git 仓库默认只保存：
  - 权威事件层
  - 少量必要元数据
- 投影结果默认作为：
  - 本地派生缓存
  - 不写回权威 Git 仓库

### 5.5 Git 协作语义

第一阶段默认采用：

- **一集体一私有仓库**
- 成员各自持有本地克隆

冲突处理默认顺序为：

1. 自动拉取
2. 自动重放 / 重试
3. 仍失败时再提示人工处理

也就是说：

- 先自动解决同步碰撞
- 不把人工冲突处理设成常态主路径

### 5.6 改账语义

第一阶段默认规则为：

- **未平账可改**
- **已平账只允许冲正 / 补记**

不采用：

- 全程自由改写
- 首版全程纯追加但不给草稿修正空间

### 5.7 UI / Agent 双入口

第一阶段保留双入口方向：

- 人类可在 UI 录入
- Agent 也可通过 API 协助录入

但边界固定为：

- Agent 默认只创建**入账草稿**
- 正式入账仍需人类确认

### 5.8 最小 API 面

第一阶段最小 API 面收口为：

- 查分类
- 建记录 / 建草稿
- 查余额 / 待平关系
- 记平账

API 默认信任边界为：

- 本机 UI
- 本机受信 Agent / 脚本

不进入首版：

- 公网多租户 API
- 复杂远程鉴权

### 5.9 最低审计字段

每条记账 / 改账记录至少带：

- 操作者
- 设备
- 时间

---

## 6. 当前明确不做的内容

第一阶段明确不做：

- 把记账系统直接并入外心主线
- 先讨论劳动记录理论细则再开始实现
- 导入旧表历史数据
- 一开始就补齐全部统计 / 看盘面板
- 公网多租户 API

---

## 7. 文档与追踪要求

这条独立系统线要求：

- 及时落盘到 Markdown 文档
- 同步到对应 issue

第一阶段追踪结构收口为：

- **一份总计划**
- **一个总 issue**

后续如该主题迁移到独立仓库，需要带着这套总计划 / 总 issue 语义一起移交。

---

## 8. 一句话总结

近期的集体记账主题不再按“外心内置账本”推进，而是按“**先做一个 Rust 内核 + 简 Web UI 的独立集体记账系统，以按月分区的事件账本和本地派生投影承接旧有核心工作流，再为后续并入外心预留规范内核与追踪文档**”来推进。
